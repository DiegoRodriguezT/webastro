---

interface Props {
	date: Date,
	title: string
}

const { date, title } = Astro.props;
---

<time datetime={date.toISOString()} transition:name={`${title} date`}>
	{
		date.toLocaleDateString('es', {
			year: 'numeric',
			month: 'numeric',
			day: 'numeric',
		})
	} 
</time>
